//funciones

function sumar(a: number, b: number){
    return a + b;
}

let resultado = sumar(3, 5);
console.log(resultado);

//si se necesitara correr en js se puede compilar el archivo .ts a .js
//ctrl + ñ terminal
//tsc fundamentos2.ts

//cuando se crea el archivo js, en el archivo de ts van a aparecer muchos errores
//pero el codigo se puede seguir ejecutando desde ts sin tener que modificar nada